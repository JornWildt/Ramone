Direct file download/upload
Forms multipart for multiple files upload(download?)
- På samme måde som form-url-encoded serialisering af object, men hvis den møder IFile eller en stream, så bliver
  indholdet af denne streamet. Altså => håndteres af passende codec
  - Så skal den recursive gennemgang af object via reflection trækkes ud (JsonFX?)

   Content-Type: multipart/form-data; boundary=AaB03x

   --AaB03x
   Content-Disposition: form-data; name="submit-name"

   Larry
   --AaB03x
   Content-Disposition: form-data; name="files"; filename="file1.txt"
   Content-Type: text/plain

   ... contents of file1.txt ...
   --AaB03x--


OK: ReaderContext
Mangler WriterContext

Check at Url og parametre bliver korrekt escaped (%, ? etc.)

Specifying what the default codec is for a type (so Accept need not be set)

Håndtering af charset i codecs
- Kan gøres i XmlCodecBase og TextCodecBase (mf?)

Predefined codecs
- Url-encoded post data
  - Mangler rekursion og datoer mm.
- Any content as a string
- Images

Ramone.HyperMedia
- Tre forskellige koncepter på tværs af media-types (to "dimensioner": hyper-media / media-types)
  - ILink
  - ILinkTemplate
  - IForm (key/value eller måske bare HTML specific med mulighed for genbrug i andre media-types)
    - SetInput(key, value), encoding etc.
    - Husk "hidden" og initial values for input
    - Submit()
- Og så diverse extension metoder til at aflæse sådanne elementer fra diverse formater
  - plus at kunne udvide det til nye formater
  - eller ILink etc. der matcher de forskellige typer
- Follow(ILink), Follow(ILinkTemplate, params)

Omdøbe Service => Client?

Skal have 
- RamoneRequest.Execute(method)
- RamoneRequest.Execute<T>(method)
- RamoneRequest.Execute(method, body)
- RamoneRequest.Execute<T>(method, body)

Test scenarie hvor der returneres "NoContent" og man forsøger at hente content (skal give null)

Gennemtænk scenarier for redirects (ved både GET og POST/PUT)
- Hvordan spiller det med Response.Created()

Authorization as first-class citizens
- Session.BasicAuthorization(uname,pwd) - a short cut for the request interceptor
- Key off IHaveInterceptors (som kommer på session, settings og service)
  - static void BasicAuthorization(this IHaveInterceptors xxx, string uname, string pwd)

Kan RamoneException.cs droppes?

- Support 
    OK:GET, OK:POST, OK:PUT, OK:DELETE, 
    HEAD, OPTIONS, PATCH

Nye Request metoder uden session (som så dannes automatisk). Gør det nemt at komme igang.
- Giver det mening? Så har man ikke nogen settings med codecs.
- Kan evt. gøres lidt mere direkte med RamoneConfiguration.NewSession()
  - Især hvis RamoneConfiguration får statiske metoder mm. som nedenfor

RamoneConfiguration
- Statiske udgaver af UserAgent, Interceptors etc. som så kopieres til service/client og igen til session

Hardcoded test URL into config

Læg type check ind i XmlCodecBase og TextCodecBase

Deafult content type (per session, service and static)
- For those json/json APIs

Before releasing:
- Include a license
- Version numbers
- Komplet README (med korrekt casing)
  - Installation
- Demo NerdDinner, twitter, flickr etc. API 

Folk vil blive forvirret af "AuthenticationHandlers" (som jo altså ikke authentikerer ...)

Docs
- Ramone 
  - Er godt for object serialization
  - Samler en række forskellige media types på ét sted
- Alle Get/Post etc. operationer er uden sideeffekter i RamoneRequest. 
  Altså kan RamoneRequest genbruges flere gange. Eller hvad ...
- Go through REST reciepes and show how its done in Ramone
- Forsøger at "tvinge" REST igennem. F.eks. ved
  - At skelne mellem media type codecs og serializere
  - Antager ikke at man KUN kan lave 1-1 serialisering med XML/JSON, men anvender derimod codecs.
  - Kun at arbejde med get/post etc. Ingen support for navngivne metoder som f.eks. "GetOrders".
- Response.Created() laver evt. GET på location

Conditional Delete, Get, etc.

Support for quality "q" param in accept header and codec selector

Der er en del som skal tænkes igennem ifm. Follow() ...

Noget med at hooke ind i redirects
- Case c = req.Get<Case>() ... ved automatisk redirect virker det, men vil man gerne vide at det er sket?
- Opsætning på både session, service/client os.

Make it possible to Follow() relative links.

Compression
- As some kind of "pipeline" contributor
  - Wrapping incoming stream in another stream (zip for instance)

Set referrer
- Manually
- Automatically when using Follow()


IService
- A place to specify additional headers that should always be included (like a API version number)
  - A shorthand for an interceptor

Async style
- http://xamlcoder.com/blog/2011/01/23/consuming-wcf-web-rest-apis-in-silverlight/
  var task = client.SendAsync(request);
  task.ContinueWith(t => ...)


Caching
- Er det smart eller skal man forvente en proxy? Det sikkert svært at få korrekt.
- Findes det allerede i .NET eller som open source
- Skal det laves med Sqlite?
- Er det pr. session eller hvad?

Authorization
- Check hvordan andre som f.eks. Hammock har gjort det.
- Standard implementations for OK:Basic, Digest, OAuth, NTLM and more
  - SimpleAuth (som sætter URL parametre ind https://github.com/restsharp/RestSharp/wiki/Authenticators)


Lots of Condition.Requries()...


- More codecs
  - vCard (kan Xyperico udgaven dresseres til at ligne XmlSerializer i .NET?)
  - Protocol buffers
  - Url-encoded post data
    - Mangler rekursion og datoer mm.
  - HAL
  - RDFa
  - RSS
  - oData


Test
 -- flere exceptions i CodecManager
 -- Fjernserveren returnerede en fejl: (405) Metoden er ikke tilladt. (POST)

Post
- Check various response codes
  - redirects
  - 500
  - 400
  - 204 - no content

Patch, Options

Codecs:
- Encoding charset?
  - Being able to set it? Or read it?
- Access to response/request headers?

Possibility to log all operations
- Configuration.Log(filename-pattern) => as an extension method on a smart interface

Versioning, different vocabularies (http://tech.groups.yahoo.com/group/rest-discuss/message/18158)
- Let serializers vary by some header element

- Demo how HTML + forms + url-encoded data can be auto-generated (a'la Eric)
  - Still missing a generic domain data embedding in HTML
  - Would be nice to see an html form autogenerated from code

Kan nogle ting som f.eks. Accept-Charset, user agent osv. sættes i app.config?

