This demo shows how to access Twitter, using OAuth1 for authentication.

In order to compile it you must first build Ramone AND run the post-build.bat
program in order to create the binaries that the demo depends on.
